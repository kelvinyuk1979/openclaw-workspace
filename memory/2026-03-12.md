# 2026-03-12

## 重要配置与决策

### 🔑 API 密钥管理
- 创建了 `api-keys.json` 和 `switch-api-key.sh` 用于主备密钥切换
- 备用 Google API 密钥 `AIzaSyCh7XMJ_P2x3l-01PFE3f-8YO1tIFM-RxA` 测试失败 (403 禁止访问)
- 需要用户在 Google Cloud Console 检查或重新生成密钥

### 🤖 模型配置
- **主模型**: `dashscope/qwen3.5-plus` (阿里云百炼)
- **备用模型**: `google/gemini-3.1-pro-preview`
- 创建了 `switch-model.sh` 脚本用于手动切换
- ⚠️ OpenClaw 2026.3.11 不支持 `agents.defaults.model.fallback` 字段，会导致配置校验失败
- ⚠️ 自定义 provider 必须在 `auth.profiles` 中添加对应条目，否则 UI 不显示
- 每次切换模型后需要运行 `openclaw gateway restart`

### 📱 小红书自动运营
- 创建了 `xiaohongshu-auto/` 目录和完整自动化系统
- 包含：内容生成器、Playwright 自动发布器、定时提醒
- 已安装 Playwright 和 Chromium 浏览器
- 发布前需要用户确认（安全考虑）
- 设置了每天 8:00/12:00/21:00 的定时提醒 Cron Job

### 📈 量化选股
- 已有 `quant-trading/` 目录和选股脚本
- 设置了工作日 9:00 自动运行 Cron Job
- 今日运行结果：0 只符合条件（市场波动小/数据源问题）

## 技能与工具

### 新增技能
- `self-improving-agent`：自我改进技能，记录错误和学习到 `.learnings/` 目录
- 通过 `clawhub install self-improving-agent` 安装
- 需要完整 PATH: `/opt/homebrew/opt/node@22/bin:/opt/homebrew/bin:/usr/local/bin:$PATH`

### 已有技能
- `akshare-stock`：A 股量化数据分析
- `sherpa-onnx-tts`：本地离线语音合成

## 技术教训 (已记入 .learnings/LEARNINGS.md)

1. OpenClaw 配置校验严格，不要添加未文档化的字段
2. 自定义 OpenAI 兼容 provider 需要同步配置 `auth.profiles`
3. clawhub CLI 需要完整 Homebrew Node 路径才能运行
4. 模型切换后必须重启 Gateway 才能生效

## 待办事项

- [ ] API 密钥：检查 Google Cloud Console 或生成新密钥
- [ ] 小红书发布：首次运行需要扫码登录
- [ ] 选股策略：考虑放宽条件或检查数据源
